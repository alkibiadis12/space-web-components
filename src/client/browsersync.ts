// import browserSync from "browser-sync";
// import path from "path";

// const bs = browserSync.create();

// bs.init({
//   server: {
//     baseDir: path.join(__dirname, "public"), // Serve files from the 'public' directory
//   },
//   files: [
//     path.join(__dirname, "public", "**", "*.*"), // Watch all files in the 'public' folder
//   ],
//   host: "localhost", // Restrict to localhost (disables external)
//   reloadDelay: 0, // Delay before reloading (optional)
// });
